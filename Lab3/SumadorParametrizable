module Sumador #(parameter n=3)(
input logic [n-1:0] a,b,
input logic cin, 
output logic [n-1:0] s,
output logic cout
    );
    assign cout = (a && b) || (cin && (a ^ b));
    assign s = cin ^ (a ^ b);
    
endmodule
